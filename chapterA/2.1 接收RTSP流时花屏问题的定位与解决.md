前面还说花屏可能是ffmpeg本身的问题。

可是我们直接使用ffplay播放却没有出现这个问题，那么我们就得看看ffplay的源码是怎样处理的，才不会产生花屏和延迟。

ffplay是否默认使用了更优的参数？

ffplay是否具备更优异的性能？

目前的思路是仿ffplay逻辑用Qt实现播放框架，但需要一个裁剪后的基础版本：

- 只保留Video相关部分，剔除Audio和Subtile。
- 使用Qt库代替SDL库实现部分功能，包括显示和线程锁等。因为不想引入SDL，Qt同样可以实现SDL的功能。

构造一个只含video的文件，看流程，把有用的转换。